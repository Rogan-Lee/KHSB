# 일일 트리아지 보고서 — 2026-04-07 (화)

**생성 시각:** 2026-04-07 09:00 KST  
**담당자:** 시스템 자동 생성

---

## ⚠️ 데이터베이스 연결 불가 (어제와 동일)

이 보고서는 데이터베이스에 접근할 수 없는 환경(샌드박스 네트워크 제한)에서 생성되었습니다.

- **오류 코드:** `P1001` — Prisma 데이터베이스 서버에 도달할 수 없음
- **엔드포인트:** `ep-frosty-night-a1240d6o-pooler.ap-southeast-1.aws.neon.tech:5432`
- **원인:** 샌드박스 환경의 이그레스 프록시가 `*.neon.tech` 도메인을 차단합니다.  
  TCP(5432), WebSocket, HTTPS HTTP API 모두 연결 불가.

데이터베이스에 접근 가능한 환경(로컬 개발 또는 Vercel)에서 아래 SQL을 실행하면 실제 항목을 확인할 수 있습니다.

---

## 어제(04-06) 이후 커밋 활동

| 시각 (KST) | 커밋 | 내용 |
|---|---|---|
| 2026-04-07 00:45 | `0c22462` | **feat:** 멘토링 목록 필터에 멘토 계정과 어드민 정지훈만 표시 |

**변경 요약:**  
`mentoring/page.tsx`의 멘토 목록 필터가 `MENTOR`, `STAFF`, `DIRECTOR`, `ADMIN` 전체 역할에서 **MENTOR 역할** 또는 **이름이 "정지훈"인 ADMIN/DIRECTOR**로 좁혀졌습니다. 멘토링 배정 UI에서 불필요한 계정이 노출되던 문제를 개선한 것으로 보입니다.

---

## 실행 예정 쿼리 (DB 접근 가능 시)

### 1. 대기/진행 중 항목 조회

```sql
SELECT
  id,
  title,
  category,
  priority,
  requester,
  "authorName",
  "createdAt"::date AS created,
  status
FROM "FeatureRequest"
WHERE status IN ('PENDING', 'IN_PROGRESS')
ORDER BY
  CASE priority
    WHEN 'URGENT' THEN 0
    WHEN 'HIGH'   THEN 1
    WHEN 'NORMAL' THEN 2
    WHEN 'LOW'    THEN 3
  END,
  "createdAt" ASC;
```

### 2. 최근 24시간 댓글 조회

```sql
SELECT
  frc.content,
  frc."authorName",
  fr.title,
  frc."createdAt"
FROM "FeatureRequestComment" frc
JOIN "FeatureRequest" fr ON fr.id = frc."featureRequestId"
WHERE frc."createdAt" > NOW() - INTERVAL '24 hours'
ORDER BY frc."createdAt" DESC;
```

---

## 미해결 항목 현황 (TODOS.md 기준)

### 🔴 P1 — 즉시 처리

| # | 항목 | 예상 공수 | 선행 조건 |
|---|------|-----------|-----------|
| 1 | Prisma `db push` → `migrate dev` 전환 | CC: ~10분 | 없음 |
| 2 | Vercel Analytics 추가 + 고객 인터뷰 30분 | CC: ~5분 + 통화 1시간 | 없음 |
| 3 | 고객 #2 확보 플레이북 (데모 영상·가격 페이지·추천 요청) | CC: ~30분 | Analytics 데이터 |

### 🟠 P2 — 중요

| # | 항목 | 유형 | 예상 공수 | 선행 조건 |
|---|------|------|-----------|-----------|
| 1 | **[보안 버그]** DailyOuting 소유권 검증 누락 | BUG | CC: ~10분 | 없음 |
| 2 | **[버그]** `getWeeklyPlanData` KST 날짜 경계 off-by-1 | BUG | CC: ~5분 | 없음 |
| 3 | **[버그]** `getMeritsByRange` 타임존 미적용 | BUG | CC: ~5분 | 없음 |
| 4 | **[버그]** 멘토링 더블클릭 중복 생성 | BUG | CC: ~10분 | Prisma migrate |
| 5 | Sentry / Vercel Error Tracking 도입 | IMPROVEMENT | CC: ~15분 | 없음 |
| 6 | 핵심 Server Action Prisma try/catch 추가 | IMPROVEMENT | CC: ~20분 | Error Tracking |
| 7 | 고객 #2 인프라 격리 런북 작성 | IMPROVEMENT | CC: ~30분 | 고객 #2 대기 |
| 8 | Kakao Friends API 상업적 이용 약관 검토 | IMPROVEMENT | 30분 리서치 | 없음 |

### 🟡 P3 — 추적

| # | 항목 | 설명 |
|---|------|------|
| 1 | 부모 리포트 만료/취소 기능 | `expiresAt` 필드 및 토큰 무효화 UI |
| 2 | Vercel Analytics 페이지뷰 추적 | `layout.tsx`에 Analytics 컴포넌트 1줄 추가 |

---

## 오늘(04-07) 권장 우선순위

1. **[P1 긴급] DailyOuting 보안 패치** — 인증된 모든 사용자가 타인의 외출 기록을 수정·삭제 가능한 취약점. 5~10분 수정, 지금 처리.
2. **[P1] KST 타임존 버그 2건** (`getWeeklyPlanData`, `getMeritsByRange`) — 각 5분 이내. 묶어서 처리.
3. **[P1] Prisma migrate 전환** — 이후 스키마 변경(`@@unique` 등)의 전제 조건. 먼저 완료해야 멘토링 중복 방지 가능.
4. **[P2] 멘토링 더블클릭 중복 방지** — migrate 전환 후 `@@unique` 제약 추가.
5. **[P1] Vercel Analytics 1줄 추가** — `layout.tsx`에 `<Analytics />` 삽입. 의존 없음, 당장 가능.

---

## 참고

- 이 보고서는 샌드박스 네트워크 제한으로 DB 실시간 데이터를 포함하지 않습니다.
- 실제 FeatureRequest 항목은 앱 내 `/feature-requests` 페이지 또는 위 SQL 쿼리로 확인하세요.
- DB 접근 가능 환경에서 실행 시 자동으로 실시간 데이터가 반영됩니다.
