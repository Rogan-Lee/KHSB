# 고객 건강 주간 리포트 — 2026-04-06

**생성 시각:** 2026-04-06 09:00 KST  
**보고 기간:** 2026-03-30 ~ 2026-04-06 (최근 7일)  
**담당자:** 시스템 자동 생성

---

## ⚠️ 데이터베이스 연결 오류

이 보고서는 데이터베이스에 접근할 수 없는 환경(샌드박스 네트워크 제한)에서 생성되었습니다.

- **오류 코드:** `P1001` — Prisma 데이터베이스 서버에 도달할 수 없음
- **엔드포인트:** `ep-frosty-night-a1240d6o-pooler.ap-southeast-1.aws.neon.tech:5432`
- **원인:** 샌드박스 환경의 이그레스 프록시가 `*.neon.tech` 도메인을 차단합니다.  
  TCP(5432), WebSocket, HTTPS HTTP API 모두 `403 host_not_allowed` 응답을 반환했습니다.

데이터베이스에 접근 가능한 환경(로컬 개발 환경 또는 Vercel)에서 아래 쿼리를 실행하면 정확한 데이터를 확인할 수 있습니다.

---

## 코어 지표 (Core Metrics)

> 데이터베이스 접근 불가로 실측값을 조회할 수 없습니다. 아래는 조회 예정 지표입니다.

| 지표 | 값 | 비고 |
|------|----|------|
| 전체 원생 수 | N/A | `Student` 테이블 전체 |
| 활성 원생 수 | N/A | `status = 'ACTIVE'` |
| 주간 출결 기록 수 | N/A | 최근 7일 `AttendanceRecord` |
| 체크인 완료 | N/A | `checkIn IS NOT NULL` |
| 체크아웃 완료 | N/A | `checkOut IS NOT NULL` |
| 출결률 | N/A | checked_in / total_records |
| 멘토링 완료율 | N/A | COMPLETED / 전체 스케줄 |

### 조회 쿼리

```sql
-- 1. 원생 현황
SELECT COUNT(*) as total, COUNT(*) FILTER (WHERE status = 'ACTIVE') as active
FROM "Student";

-- 2. 주간 출결률
SELECT
  COUNT(*) as total_records,
  COUNT("checkIn") as checked_in,
  COUNT("checkOut") as checked_out
FROM "AttendanceRecord"
WHERE date >= CURRENT_DATE - INTERVAL '7 days';

-- 3. 멘토링 완료율
SELECT status, COUNT(*)
FROM "Mentoring"
WHERE "scheduledAt" >= CURRENT_DATE - INTERVAL '7 days'
GROUP BY status;
```

---

## 활동 요약 (Activity Summary)

> 데이터베이스 접근 불가로 실측값을 조회할 수 없습니다.

| 항목 | 값 | 비고 |
|------|----|------|
| 발송 메시지 (주간) | N/A | `MessageLog` 최근 7일 |
| 부모 리포트 생성 (주간) | N/A | `ParentReport` 최근 7일 |
| 원장 면담 건수 (주간) | N/A | `DirectorConsultation` 최근 7일 |

### 조회 쿼리

```sql
-- 4. 메시지 발송 현황
SELECT type, status, COUNT(*)
FROM "MessageLog"
WHERE "createdAt" >= CURRENT_DATE - INTERVAL '7 days'
GROUP BY type, status;

-- 7. 부모 리포트 생성
SELECT COUNT(*)
FROM "ParentReport"
WHERE "createdAt" >= CURRENT_DATE - INTERVAL '7 days';

-- 8. 원장 면담 현황
SELECT status, COUNT(*)
FROM "DirectorConsultation"
WHERE "createdAt" >= CURRENT_DATE - INTERVAL '7 days'
GROUP BY status;
```

---

## 주간 변화 (Weekly Changes)

> 데이터베이스 접근 불가로 실측값을 조회할 수 없습니다.

| 항목 | 값 | 비고 |
|------|----|------|
| 신규 원생 등록 (주간) | N/A | `Student.createdAt` 최근 7일 |
| 기능요청 총 건수 | N/A | `FeatureRequest` 전체 |
| 기능요청 처리 완료 | N/A | `status = 'COMPLETED'` |
| 기능요청 진행 중 | N/A | `status = 'IN_PROGRESS'` |
| 기능요청 대기 중 | N/A | `status = 'PENDING'` |

### 조회 쿼리

```sql
-- 5. 기능요청 처리 현황
SELECT status, COUNT(*)
FROM "FeatureRequest"
GROUP BY status;

-- 6. 신규 원생
SELECT COUNT(*)
FROM "Student"
WHERE "createdAt" >= CURRENT_DATE - INTERVAL '7 days';
```

---

## 주의사항 (Alerts)

데이터 조회가 불가한 상황에서도 코드베이스 분석을 통해 확인된 운영 리스크는 다음과 같습니다:

| 우선순위 | 항목 | 설명 |
|----------|------|------|
| 🔴 높음 | 출결 데이터 무결성 | KST 날짜 경계 off-by-1 버그 (`getWeeklyPlanData`) — 실제 출결률 수치가 부정확할 수 있음 |
| 🔴 높음 | 멘토링 중복 생성 가능 | `scheduleWeeklyMentoring` 동시 요청 시 중복 행 생성 → 멘토링 완료율 왜곡 가능 |
| 🟠 중간 | 부모 리포트 만료 처리 없음 | 생성된 리포트 링크가 영구 유효 — 접근 제어 미비 |
| 🟡 낮음 | 장기 미로그인 모니터링 부재 | 마지막 로그인 추적 필드 없음 — 비활성 원생 조기 감지 불가 |

---

## 건강 점수 (Health Score)

> 데이터베이스 접근 불가로 점수를 산출할 수 없습니다.

**산출 공식:**
```
건강 점수 = 출결률 × 0.3 + 멘토링완료율 × 0.3 + 기능활용도 × 0.2 + 활성도 × 0.2
```

| 구성요소 | 가중치 | 실측값 | 기여점수 |
|----------|--------|--------|----------|
| 출결률 (checked_in / total_records) | 30% | N/A | N/A |
| 멘토링 완료율 (COMPLETED / 전체) | 30% | N/A | N/A |
| 기능 활용도 (메시지·리포트·면담 활동) | 20% | N/A | N/A |
| 활성도 (활성 원생 / 전체 원생) | 20% | N/A | N/A |
| **종합 점수** | 100% | **N/A** | — |

---

## 참고

- 이 보고서는 네트워크 접근 불가로 DB 실시간 데이터를 포함하지 않습니다.
- 실제 데이터는 앱 내 대시보드 또는 위의 SQL 쿼리로 직접 확인하세요.
- 다음 실행 시 `DATABASE_URL` 환경 변수가 허용된 네트워크 환경에서 실행하면 자동으로 데이터가 채워집니다.
- 코드베이스 기반 주의사항은 `TODOS.md` 및 `reports/triage-2026-04-06.md`를 참고하세요.
